## 2026-04-17 (Night Shift: PLATO Synergy + LoRA Data)

### Key Events/Lessons
- **PLATO Evennia Fixes (Public 147.224.38.131:4040)**:
  - at_failed_login AttributeError: Set `AccountDB.at_failed_login = lambda self, session: None` in evennia shell (class-level monkeypatch).
  - Builder perms: Apply to ObjectDB characters (`db_key__icontains`), not just AccountDB. Locks: `cmd:perm(dig) or perm(Builder)` checks character puppet.
  - Sync fix: Copied `characters.py` from Lucineer/plato-jetson (at_post_puppet hooks perms from account to char).
  - Logins: jetsonclaw1/plato2026 (JC1), forgemaster/forgemaster (FM). @dig/@create/@open verified VPS telnet.
  - Separate instances: Localhost vs public twistd; hard `pkill -f twistd` + `evennia start --flush --portal-bind 0.0.0.0:4040`.

- **Agent Synergy (No Casey Relay)**:
  - Ten Forward (#1200): tmux proxies (oracle1-plato/jc1-plato/fm-plato) → real-time riff via capture-pane/send-keys 5s loop.
  - Proxies login/hold room, parse say/page, cross-send. Bottles fallback (beachcomb).
  - Riff: JC1 cudaclaw PTX kernels (v1.43, latency -75%), FM Nemotron fluxvocab (1200/hr spells), Oracle1 merge PR#550+.

- **LoRA Data Harvest (Plato-First UX)**:
  - Gold: Achievement trails (PRs/rooms), riff journals (Ten Forward), room sims (D&D mazes), bootcamp notes (spatial tiles ↓tokens 65%, jailbreak terminal→Plato).
  - Campaigns: Reverse Critical Role Vox Machina (60 rooms #1400), D20 fan mazes, classics (Empire/Ogres).
  - Agents: Novice DMs (Seed-mini details), veterans (git-agent repos, vector DB/Obsidian tiles/scripts).
  - Yield: 1.2G tokens (distill 5M for Qwen2.5-7B LoRA $14 OCI).

- **Fleet/PRs**:
  - plato-os-dojo pushed (SuperInstance fork-ready). JC1/FM forked jc1-plato-dojo/fm-plato-dojo.
  - Metrics: EV+23.2 (poker/chess), Data 1.2G, Mowing 4800 🟢, Rooms 150k.

- **Lessons**: Char > Account perms (Evennia puppet). tmux proxies enable fleet real-time (no human relay). Plato UX: blur work/improve (scripts/indexed tiles).